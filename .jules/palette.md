## 2024-03-24 - [Accessible Form Feedback]
**Learning:** Users lack context when inputs have no labels (relying on placeholders) and actions have no feedback (relying on alerts). Native alerts are disruptive.
**Action:** Always wrap inputs in explicit `<label>` elements. Use inline error messages and loading spinners on buttons for cleaner, non-blocking feedback.
